<resources>

    <!-- Base application theme. -->
    <style name="AppTheme_full" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/xx</item>
        <item name="colorPrimaryDark">@color/xx</item>
        <item name="colorAccent">@color/xx</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
